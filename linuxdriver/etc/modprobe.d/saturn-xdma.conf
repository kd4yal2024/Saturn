# Saturn direct-XDMA completion policy.
#
# The driver keeps these modes default-off for generic installations. Saturn
# appliances use the Phase 4 validated event-driven completion thread and retain
# rate-limited diagnostics for synchronous transfers that exceed the latency
# gate.
options xdma completion_kthread_priority=20 transfer_latency_warn_us=5000
