# Watchdog RTL gate

`activitywatchdog.v` is linted by `../scripts/lint.sh` and proved by
`../../formal/watchdog.sby`. Add simulation-only watchdog tests here when a
cycle-accurate regression beyond the formal contract is needed.
